#include <cstdio>

int ugad(int n);

int main()
{
    int n = 0;
    FILE* tmp = fopen("input.txt", "r");
    fscanf(tmp, "%d", &n);
    fclose(tmp);
    tmp = fopen("output.txt", "w");
    int ans = ugad(n);
    fprintf(tmp, "%d", ans);
    fclose(tmp);
    return 0;
}

int ugad(int n)
    {
    int ans = n - 1;
    while(i)
    return ans;
    }
